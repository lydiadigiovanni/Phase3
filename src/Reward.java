import javafx.scene.image.Image;

public class Reward {
    private Image goldReward = new Image("K-Counting-Bronze-Medal.png");
    private Image silverReward = new Image("K-Counting-Gold-Medal.png");
    private Image bronzeReward = new Image("K-Counting-Silver-Medal.png");
    private int testGrade;

    /*public ? getReward() {
        if (testGrade >= 90 || testGrade <= 100) {
            return goldReward;
        }
        else if (testGrade >= 80 || testGrade <= 89)
        {
            return silverReward;
        }
        else if (testGrade >= 65 || testGrade <= 79 )
        {
            return bronzeReward;
        }
        

    }*/
    
    
}
